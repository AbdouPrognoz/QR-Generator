{
  "name": "qr-generator",
  "version": "1.0.0",
  "description": "using npm modules we will be able to generate qr codes from user inputs",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "abdou",
  "license": "ISC",
  "dependencies": {
    "inquirer": "^12.9.0",
    "qr-image": "^3.2.0"
  }
}
